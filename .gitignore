# Build artifact for Netlify Drop — regenerate with ./build-drop-zip.sh, never commit (19MB)
salty-schooner-app.zip

# Local Netlify folder
.netlify

# Node dependencies for the Netlify Functions backend
node_modules

# Netlify Edge Functions cache (Deno)
deno.lock

# macOS
.DS_Store
